#ifndef __LIB_H__
#define __LIB_H__

// Declaración:

/* La declaración de una función nos define el tipo
 * de dato que regresa la función, el nombre de la misma
 * y los parámetros y el tipo que recibe.
 */

    int func1(int a, int b);
    char func2(const char* str);
    int func3(int x, int y);

#endif
